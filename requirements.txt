# ---- Core (required) ----
pandas>=2.0
numpy>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
streamlit>=1.30
plotly>=5.18
requests>=2.31

# ---- NLP (optional but recommended — enables real FinBERT + spaCy) ----
# The system runs without these via built-in fallbacks, but install them
# for production-grade sentiment and entity recognition.
transformers>=4.38
torch>=2.1
spacy>=3.7
# After install run:  python -m spacy download en_core_web_sm

# ---- Semantic dedup (optional) ----
# faiss-cpu>=1.7
# sentence-transformers>=2.5

# ---- Production streaming / DB (optional) ----
# confluent-kafka>=2.3
# psycopg2-binary>=2.9
# sqlalchemy>=2.0

# ---- Testing ----
pytest>=8.0
hypothesis>=6.98
